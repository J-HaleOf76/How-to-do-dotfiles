[user]
    name = J-HaleOf76
    email = johnhale1976@live.com
[alias]
    ad = add .
    br = branch
    ci = commit
    co = checkout
    lo = log --oneline -n 15
    st = status
    up = pull --rebase
    re = restore
[push]
    default = current
[core]
    editor = nvim -c 'put_' -c 'startinsert'
    excludesfile = ~/.config/.global-gitignore
[rebase]
    autoStash = true
[init]
    defaultBranch = master
